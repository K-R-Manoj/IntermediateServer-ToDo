_414‍.x([["errorHandle",()=>errorHandle],["errorFirst",()=>errorFirst],["isJsonString",()=>isJsonString]]);const errorHandle = (func) => (req, res, next) => {
    Promise.resolve(func(req, res, next)).catch(next);
};

       const errorFirst = (promise) => promise.then((x) => [null, x]).catch((x) => [x, {}]);

       const isJsonString = (str) => {
    try {
      JSON.parse(str);
    } catch (e) {
      return false;
    }
    return true;
};